# external-destinations

Template for using the library to send data to a destination outside the cluster.  
Application monitoring is based on the default ServiceMonitor only.  

Deployment schema: first apply `setup.jsonnet` then `monitoring.jsonnet`
